<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Link Shortener Ads" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="manifest" href="/manifest.json">


    <link rel="icon" href="<%= asset_path('icons.png')%>" type="image/png">
    

     <link rel="icon" href="<%= asset_path('icons.png') %>" type="image/x-icon">  
    <link rel="apple-touch-icon" href="<%= asset_path('icons.svg')%>" sizes="192x192">
     <link rel="icon" href="<%= asset_path('icons.svg')%>" type="image/svg+xml">
     
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Lily+Script+One&display=swap" rel="stylesheet">
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", type: "module" %>
  </head>

  <body class="bg-gray-50">
  <%= render 'shared/navbar' %>
  
  <%= render 'shared/notefy' %>
  
      <%= yield %>
  <%= render 'shared/footer' %>
</body>

</html>
